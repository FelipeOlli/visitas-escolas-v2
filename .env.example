# Banco de dados
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/visitas

# NextAuth (gere o secret: openssl rand -base64 32)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Google Maps (chave pública — restringir por HTTP Referrer no Google Cloud Console)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
